# MF Salon — Sprint 1 (Core / Architecture Proof)

Objectif: prouver que Next.js + Netlify build + routing fonctionnent, sans Supabase.

## Routes
- `/` : OK — Home
- `/health` : OK — Health
- `/bo` : OK — Back-office shell

## Local
```bash
npm install
npm run dev
```

## Netlify (IMPORTANT)
- Base directory: (vide)
- Publish directory: (vide)
- Functions directory: (vide)
- Branch: main
Le plugin `@netlify/plugin-nextjs` gère le déploiement.
